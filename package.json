{
  "name": "quickshare",
  "version": "0.0.0",
  "private": true,
  "description": "Browser extension to copy page links in Slack, Markdown, or Confluence format.",
  "license": "ISC",
  "type": "module",
  "author": "",
  "scripts": {
    "lint": "eslint --ext .js,.jsx,.ts,.tsx",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.0.3",
    "semantic-release": "^24.0.0"
  },
  "release": {
    "branches": ["main"],
    "plugins": [
      "@semantic-release/commit-analyzer",
      "@semantic-release/release-notes-generator",
      "@semantic-release/github"
    ]
  }
}
